flask==3.0.3
flask-jwt-extended==4.6.0
flask-pymongo==2.3.0
pymongo==4.7.3
python-dotenv==1.0.1
bcrypt==4.1.3
deepface==0.0.93
opencv-python==4.10.0.84
reportlab==4.2.2
qrcode[pil]==7.4.2
Pillow==10.4.0
groq==0.9.0
cloudinary==1.40.0
python-dateutil==2.9.0
shortuuid==1.0.13
gunicorn==22.0.0

# ── Production additions ───────────────────────────────────────────
Flask-Limiter==3.7.0          # Rate limiting on login endpoints
limits==3.13.0                # Flask-Limiter backend
flask-talisman==1.1.0         # Security headers (CSP, HSTS, X-Frame-Options, etc.)
sentry-sdk[flask]==2.12.0     # Error monitoring + alerting
Flask-Mail==0.10.0            # Email notifications
filetype==1.2.0               # Magic-byte MIME validation (pure Python, no libmagic)
itsdangerous==2.2.0           # Timed tokens for password-reset links (already bundled with Flask, pinned for clarity)

# ── Optional: shared rate-limit storage for multi-worker deployments ──────
# Uncomment if running with Gunicorn --workers > 1 and set
# RATELIMIT_STORAGE_URI=redis://<host>:6379/0 in .env — without this,
# each worker keeps its own in-memory rate-limit counters (see config.py
# check_rate_limit_storage() for details).
# redis==5.0.8
<<<<<<< HEAD
pdfplumber==0.10.3
=======
>>>>>>> 091fbe1a0bfbb2d98bc394e9b2093ff6a720c55c
